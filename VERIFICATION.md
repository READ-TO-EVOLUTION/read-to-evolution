# 動作確認ガイド

## 初回起動手順（Windows PowerShell）

### 1. 依存関係のインストール

```powershell
npm install
```

### 2. 環境変数の設定

プロジェクトルートに `.env` ファイルを作成：

**方法1: PowerShellで作成**
```powershell
@"
DATABASE_URL="file:./dev.db"
JWT_SECRET="dev-secret-key-change-in-production-min-32-chars"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
"@ | Out-File -FilePath .env -Encoding utf8
```

**方法2: 手動で作成**
プロジェクトルートに `.env` ファイルを新規作成し、以下の内容をコピー＆ペースト：

```env
DATABASE_URL="file:./dev.db"
JWT_SECRET="dev-secret-key-change-in-production-min-32-chars"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

### 3. データベースの初期化

```powershell
npm run db:generate
npm run db:push
```

**確認**: `prisma/dev.db` ファイルが作成されていることを確認してください。

### 4. 開発サーバーの起動

```powershell
npm run dev
```

ブラウザで [http://localhost:3000](http://localhost:3000) を開きます。

---

## 事前準備（詳細版）

### 1. 依存関係のインストール

```bash
npm install
```

### 2. 環境変数の設定

プロジェクトルートに `.env` ファイルを作成：

```env
DATABASE_URL="file:./dev.db"
JWT_SECRET="dev-secret-key-change-in-production-min-32-chars"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

### 3. データベースの初期化

```bash
npx prisma generate
npx prisma db push
```

### 4. 開発サーバーの起動

```bash
npm run dev
```

ブラウザで [http://localhost:3000](http://localhost:3000) を開きます。

---

## 動作確認手順

### ステップ1: ユーザー登録

1. [http://localhost:3000/register](http://localhost:3000/register) にアクセス
2. メールアドレスとパスワード（6文字以上）を入力
3. 「登録」をクリック
4. ログイン画面にリダイレクトされることを確認

**確認ポイント**:
- ✅ 登録成功時にログイン画面へ遷移
- ✅ 既存メールアドレスで登録しようとするとエラー表示

---

### ステップ2: ログイン

1. [http://localhost:3000/login](http://localhost:3000/login) にアクセス
2. 登録したメールアドレスとパスワードを入力
3. 「ログイン」をクリック
4. トップページまたは「今日の復習」画面に遷移することを確認

**確認ポイント**:
- ✅ ログイン成功時にトップページへ遷移
- ✅ 間違ったパスワードでエラー表示
- ✅ ログイン後、トップページにメールアドレスが表示される

---

### ステップ3: 書籍登録

1. トップページから「教材管理」をクリック、または [http://localhost:3000/books](http://localhost:3000/books) に直接アクセス
2. 「+ 書籍を登録」をクリック
3. 書籍名を入力（例：「基本情報技術者試験 過去問集」）
4. 「登録」をクリック
5. 書籍一覧に追加されることを確認

**確認ポイント**:
- ✅ 書籍が一覧に表示される
- ✅ 冊数上限チェック（無料プランは登録できない、または制限あり）

---

### ステップ4: 学習記録の登録

1. 書籍一覧から「教材を登録」をクリック
2. 以下の情報を入力：

   **必須項目**:
   - 問題画像URL: `https://via.placeholder.com/400x300?text=問題`
   - 位置情報: `p.45` など

   **任意項目**:
   - 答え画像URL: `https://via.placeholder.com/400x300?text=答え`
   - 解説画像URL: `https://via.placeholder.com/400x300?text=解説`
   - メモ: 「重要ポイント」など

3. 位置情報のタイプを選択（ページ/問題/自由/章/節）
4. 「登録」をクリック
5. 登録成功メッセージが表示されることを確認

**確認ポイント**:
- ✅ 必須項目が未入力の場合、エラー表示
- ✅ 位置情報の前回値が自動セットされる（2回目以降）
- ✅ 登録後、フォームがリセットされる

**テスト用画像URL**:
- 問題: `https://via.placeholder.com/400x300?text=問題1`
- 答え: `https://via.placeholder.com/400x300?text=答え1`
- 解説: `https://via.placeholder.com/400x300?text=解説1`

---

### ステップ5: 今日の復習一覧の確認

1. 「今日の復習」画面にアクセス: [http://localhost:3000/study/today](http://localhost:3000/study/today)
2. 登録した学習記録が表示されることを確認

**確認ポイント**:
- ✅ 書籍名が表示される
- ✅ 位置情報が表示される（例：「ページ p.45」）
- ✅ 問題画像が表示される
- ✅ 「復習を開始」ボタンが表示される
- ✅ Step情報が表示される

**注意**: Step1-2の場合は「今日の復習」に表示されません（Step3以上のみ表示）

---

### ステップ6: 復習の実行（Step3-6）

1. 「今日の復習」画面で「復習を開始」をクリック
2. **問題表示画面**:
   - 問題画像が表示されることを確認
   - 「思い出してみる」ボタンをクリック

3. **想起確認画面**:
   - 3色ボタンが表示されることを確認
     - 🟢 思い出せた
     - 🟡 あいまい
     - 🔴 思い出せない
   - いずれかのボタンをクリック

4. **答え・解説表示画面**:
   - 答え画像と解説画像が表示されることを確認
   - 「合っていた」または「合っていなかった」を選択

5. **結果確認画面**:
   - 結果が表示されることを確認
   - 「保存して完了」をクリック

6. 「今日の復習」画面に戻ることを確認

**確認ポイント**:
- ✅ 各フェーズが正しく進行する
- ✅ 画像が正しく表示される
- ✅ ボタンが正しく動作する
- ✅ 保存後、今日の復習一覧から該当項目が消える（次回復習日が設定される）

---

### ステップ7: 復習スケジュールの確認

1. 復習を実行後、Prisma Studioで確認：

```bash
npm run db:studio
```

2. ブラウザで [http://localhost:5555](http://localhost:5555) を開く
3. `review_schedules` テーブルを確認
4. `next_review_at` が正しく設定されていることを確認

**確認ポイント**:
- ✅ Step3成功 → Step4（+4日後）
- ✅ Step4成功 → Step5（+6日後）
- ✅ Step5成功 → Step6（+10日後）
- ✅ Step6成功 → Step6継続（+10日後）
- ✅ 失敗 → Step2（翌日）

---

### ステップ8: 卒業（MASTERED）の確認

1. Step6で復習を実行し、「合っていた」を選択
2. 卒業メッセージが表示されることを確認：
   - 「✓ この内容は、あなたの知識として定着しました。」

3. Prisma Studioで確認：
   - `study_records` テーブルで `state` が `mastered` になっている
   - `review_schedules` テーブルで `review_type` が `refresh` のスケジュールが作成されている
   - `next_review_at` が30日後に設定されている

**確認ポイント**:
- ✅ 卒業メッセージが表示される
- ✅ 状態が `mastered` に変更される
- ✅ 再確認スケジュールが作成される

---

### ステップ9: 再確認（REFRESH）の確認

1. Prisma Studioで `review_schedules` テーブルを開く
2. `review_type` が `refresh` のレコードを編集
3. `next_review_at` を今日の日付に変更して保存
4. 「今日の復習」画面をリロード
5. 「再確認」セクションに表示されることを確認
6. 再確認を実行し、成功/失敗の動作を確認

**確認ポイント**:
- ✅ 再確認が「今日の復習」に表示される
- ✅ 成功時：次の再確認日が設定される（30→90→180日周期）
- ✅ 失敗時：Step2に戻り、通常復習に戻る

---

### ステップ10: 読書進捗入力の確認（Phase 1.5）

1. 書籍一覧画面（[http://localhost:3000/books](http://localhost:3000/books)）にアクセス
2. 書籍カードに「進捗入力」ボタン（オレンジ色）が表示されることを確認
3. 「進捗入力」をクリック
4. 進捗入力画面が表示されることを確認

**新規進捗登録**:
1. ページ数または読了率のどちらか（または両方）を入力
   - ページ数: 正の整数（例：150）
   - 読了率: 0.0〜100.0（例：45.5）
2. 「進捗を登録」をクリック
3. 登録成功メッセージが表示され、書籍一覧に戻ることを確認

**既存進捗更新**:
1. 同じ書籍の「進捗入力」を再度クリック
2. 前回入力した値がフォームに表示されることを確認
3. 「最終更新」の日時が表示されることを確認
4. 値を変更して「進捗を更新」をクリック
5. 更新成功メッセージが表示されることを確認

**バリデーション確認**:
1. 両方未入力で送信 → エラーメッセージが表示される
2. ページ数に負の値を入力 → エラーメッセージが表示される
3. 読了率に100を超える値を入力 → エラーメッセージが表示される

**データベース確認**:
1. Prisma Studioで `reading_progress` テーブルを確認
2. `user_id` + `book_id` で1件のみ存在することを確認（ユニーク制約）
3. `pages_read` または `percent_read` が正しく保存されていることを確認
4. `updated_at` が更新されていることを確認

**確認ポイント**:
- ✅ ページ数のみ入力可能
- ✅ 読了率のみ入力可能
- ✅ ページ数と読了率の両方入力可能
- ✅ 両方未入力はエラー
- ✅ 既存進捗がある場合、前回値が表示される
- ✅ 更新時に `updated_at` が更新される
- ✅ 同じ書籍で複数回更新可能（1件に制限される）

---

## データベース確認方法

### Prisma Studio

```bash
npm run db:studio
```

ブラウザで [http://localhost:5555](http://localhost:5555) が開きます。

### 確認すべきテーブル

1. **users**: ユーザー情報
2. **books**: 登録した書籍
3. **assets**: アップロードした画像URL
4. **study_records**: 学習記録（Step、State、位置情報）
5. **review_schedules**: 復習スケジュール（通常/再確認）
6. **review_logs**: 復習実行ログ
7. **reading_progress**: 読書進捗（Phase 1.5）

---

## トラブルシューティング

### エラー: "認証が必要です"

- ログイン状態を確認
- ブラウザのクッキーを確認
- `.env` の `JWT_SECRET` が設定されているか確認

### エラー: "書籍が見つかりません"

- 書籍が正しく登録されているか確認
- ユーザーIDが一致しているか確認（Prisma Studioで確認）

### 画像が表示されない

- 画像URLが正しいか確認
- CORSエラーが出ていないか確認（ブラウザのコンソール）
- テスト用には `https://via.placeholder.com/` を使用

### 今日の復習に表示されない

- `review_schedules` テーブルで `next_review_at` が今日の日付になっているか確認
- Stepが3以上か確認（Step1-2は表示されない）
- ブラウザの日付設定を確認

### データベースエラー

```bash
# データベースをリセット（注意：全データが削除されます）
rm prisma/dev.db
npx prisma db push
```

---

## 確認チェックリスト

### 基本機能
- [ ] ユーザー登録
- [ ] ログイン/ログアウト
- [ ] 書籍登録
- [ ] 学習記録登録
- [ ] 位置情報の自動セット

### 復習機能
- [ ] 今日の復習一覧表示
- [ ] 通常復習の実行
- [ ] 想起確認（3色ボタン）
- [ ] 答え/解説表示
- [ ] 最終判定
- [ ] スケジュール更新

### 卒業機能
- [ ] Step6連続成功で卒業
- [ ] 卒業メッセージ表示
- [ ] 再確認スケジュール作成

### 再確認機能
- [ ] 再確認が一覧に表示
- [ ] 再確認の実行
- [ ] 成功時のスケジュール更新
- [ ] 失敗時のStep2への復帰

### 読書進捗入力（Phase 1.5）
- [ ] 進捗入力画面へのアクセス
- [ ] ページ数のみ入力
- [ ] 読了率のみ入力
- [ ] ページ数と読了率の両方入力
- [ ] 既存進捗の更新
- [ ] バリデーション（両方未入力エラー）
- [ ] バリデーション（ページ数負の値エラー）
- [ ] バリデーション（読了率範囲外エラー）

### 著作権配慮
- [ ] 他ユーザーのデータにアクセスできない
- [ ] 運営による教材配信機能がない
- [ ] ユーザー投稿のみ利用

---

## 次のステップ

動作確認が完了したら：

1. **レビュー機能の実装**（MVP後半）
2. **ファイルアップロード機能**（現在はURL入力）
3. **通知機能**
4. **プラン管理・決済機能**
