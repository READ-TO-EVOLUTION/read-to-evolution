# 書籍登録エラー完全解決手順

## 現在の状況

- ✅ データベースは同期済み（`user_books`テーブルは作成済み）
- ✅ 開発サーバーはポート3001で起動中
- ⚠️ ポート3000は別のプロセス（PID: 3008）が使用中
- ⚠️ Prisma Clientの生成でファイルロックエラー

## 解決手順

### オプション1: ポート3001で動作確認（推奨・簡単）

**現在、開発サーバーはポート3001で起動しています。**

1. **ブラウザで以下を開く**:
   ```
   http://localhost:3001/books
   ```

2. **書籍登録を試す**
   - ログインしていることを確認
   - 書籍登録フォームでタイトルを入力して登録

3. **エラーが出た場合**
   - ブラウザのコンソール（F12）でエラーメッセージを確認
   - エラーメッセージを共有してください

### オプション2: Prisma Clientを再生成（完全解決）

**ステップ1: 開発サーバーを停止**

ターミナルで`npm run dev`を実行している場合、**Ctrl+C**を押して停止

**ステップ2: Prisma Clientを再生成**

```bash
npx prisma generate
```

**期待される出力**:
```
✔ Generated Prisma Client
```

**ステップ3: 開発サーバーを再起動**

```bash
npm run dev
```

**ステップ4: 書籍登録を試す**

- `http://localhost:3000/books` または `http://localhost:3001/books` を開く
- 書籍登録を試す

### オプション3: ポート3000を解放（オプション）

ポート3000を使用しているプロセスを終了したい場合：

```powershell
# プロセスID 3008を終了
taskkill /PID 3008 /F
```

その後、開発サーバーを再起動するとポート3000で起動します。

## 重要な確認事項

### 現在のURL

開発サーバーがポート3001で起動している場合：
- **書籍登録画面**: `http://localhost:3001/books`
- **ログイン画面**: `http://localhost:3001/login`

### エラーが出た場合

ブラウザの開発者ツール（F12）を開き、以下を確認：

1. **Consoleタブ**: エラーメッセージの内容
2. **Networkタブ**: `/api/books` のリクエスト
   - ステータスコード
   - レスポンスボディ

エラーメッセージの内容を共有してください。

---

**修正ファイル**:
- `prisma/schema.prisma` - `datasource db.url`を`env("DATABASE_URL")`に変更

**実行したコマンド**:
- `npx prisma db push --skip-generate` - 成功（データベースは同期済み）
