# 通知機能の改善内容

## 変更ファイル一覧

### 1. データベーススキーマ
- **`prisma/schema.prisma`**
  - `Notification` モデルに `nextReviewAt` フィールドを追加
  - 重複抑制のためのunique制約を追加: `@@unique([userId, studyRecordId, type, nextReviewAt])`

### 2. 通知生成サービス
- **`lib/notification-service.ts`**
  - 通知タイトル生成ロジックを修正（接頭辞を削除）
  - `nextReviewAt` パラメータを必須化
  - 重複チェック機能を追加（既存通知があればスキップ）

### 3. 復習実行API
- **`app/api/study-records/[id]/review/route.ts`**
  - 通知生成時に `nextReviewAt` を渡すように修正
  - すべての通知生成箇所で `nextReviewAt` を取得して渡す

### 4. Step1-2進行API
- **`app/api/study-records/[id]/step1-2/route.ts`**
  - 通知生成時に `nextReviewAt` を渡すように修正

### 5. 通知画面
- **`app/notifications/page.tsx`**
  - `nextReviewAt` を表示するように修正
  - 既読操作後にヘッダーの未読数を更新

### 6. 共通ヘッダーコンポーネント
- **`components/AppHeader.tsx`**（新規作成）
  - 未読数バッジを表示
  - ページ遷移時に未読数を更新
  - 10秒ごとにポーリングで未読数を更新
  - グローバル関数 `updateUnreadCount` を公開

### 7. レイアウト
- **`app/layout.tsx`**
  - 全画面に `AppHeader` を組み込み

### 8. 復習画面
- **`app/study/review/[id]/page.tsx`**
  - 復習完了後にヘッダーの未読数を更新

### 9. 学習記録登録画面
- **`app/books/[id]/materials/page.tsx`**
  - 学習記録作成後にヘッダーの未読数を更新

---

## 主な変更内容

### 1. 通知タイトル仕様準拠

**変更前:**
```
【復習】基本情報技術者試験 過去問集｜ページ p.45
```

**変更後:**
```
【復習】基本情報技術者試験 過去問集｜p.45
```

- `location_value` 以外の接頭辞（「ページ」「章」など）を削除
- `location_value` が空の場合は「（位置未設定）」を表示

### 2. 重複抑制

**実装内容:**
- DBのunique制約: `@@unique([userId, studyRecordId, type, nextReviewAt)`
- 通知生成前に既存通知をチェック
- 同一条件の通知が既に存在する場合はスキップ

**効果:**
- 同じ復習スケジュールに対して重複した通知が作成されない
- データベースの整合性が保たれる

### 3. 次回復習日時の表示

**追加内容:**
- 通知一覧に「次回復習: {日時文字列}」を表示
- 例: 「次回復習: 2024/01/15 10:00」

### 4. 全画面共通ヘッダー＋未読数バッジ

**実装内容:**
- `AppHeader` コンポーネントを新規作成
- すべてのページにヘッダーを表示
- 未読通知数がバッジで表示される
- ページ遷移時に未読数を自動更新
- 10秒ごとにポーリングで未読数を更新
- 通知操作（既読など）後に未読数を即座に更新

**表示例:**
```
[通知 (3)] ← 未読数がバッジで表示
```

---

## 動作確認手順（更新版）

### 1. データベーススキーマの更新（必須）

```bash
npx prisma generate
npx prisma db push
```

**重要**: 既存のデータベースがある場合、`Notification` テーブルに `nextReviewAt` カラムが追加され、unique制約が設定されます。

**注意**: 既存の通知データがある場合、unique制約違反が発生する可能性があります。その場合は：
- Prisma Studioで重複データを削除
- または、データベースをリセット（開発環境のみ）

### 2. 開発サーバーの起動

```bash
npm run dev
```

### 3. 通知タイトル仕様の確認

1. 学習記録を作成（location_value: `p.45`）
2. 通知画面を確認
3. **タイトルが「【復習】{書籍名}｜p.45」になっているか確認**（「ページ」が付いていない）

### 4. 重複抑制の確認

1. 同じ復習を実行（同じnextReviewAtになるように）
2. 通知画面を確認
3. **同じ通知が重複して作成されていないか確認**
4. Prisma Studioで `notifications` テーブルを確認
5. **同一条件の通知が1件のみであることを確認**

### 5. 次回復習日時の表示確認

1. 通知画面を開く
2. **各通知に「次回復習: {日時}」が表示されているか確認**

### 6. ヘッダー未読数バッジの確認

1. 未読通知がある状態で任意のページに移動
2. **ヘッダーに未読数バッジが表示されるか確認**
   - 例: 「通知 (3)」のように赤いバッジが表示される
3. 通知画面で通知を既読にする
4. **ヘッダーの未読数バッジが即座に減ることを確認**（即時更新）
5. 「すべて既読にする」をクリック
6. **ヘッダーの未読数バッジが消えることを確認**

### 7. 通知生成時の未読数更新確認

1. 復習を実行して通知を生成
2. **復習完了後、ヘッダーの未読数バッジが増えることを確認**（即時更新）
3. 別のタブで通知を生成
4. **10秒以内にヘッダーの未読数が更新されることを確認**（ポーリング）

### 8. ページ遷移時の未読数更新確認

1. 未読通知がある状態で「今日の復習」→「教材管理」などページ遷移
2. **ページ遷移時にヘッダーの未読数が更新されることを確認**

---

## トラブルシューティング

### データベースエラー: unique制約違反

**原因**: 既存の通知データと新しいunique制約が競合

**対処法**:
```bash
# 既存の通知データを削除（開発環境のみ）
npx prisma studio
# notifications テーブルを開いて、重複データを手動削除

# または、データベースをリセット（注意：全データが削除されます）
rm prisma/dev.db
npx prisma db push
```

### ヘッダーの未読数が更新されない

**確認事項**:
1. `AppHeader` コンポーネントが正しく読み込まれているか
2. ブラウザのコンソールでエラーが出ていないか
3. `/api/notifications` APIが正常に動作しているか

**対処法**:
- ブラウザをリロード
- 開発サーバーを再起動

### 通知タイトルに接頭辞が残っている

**確認事項**:
- `lib/notification-service.ts` の `createReviewNotification` 関数を確認
- `locationTypeMap` を使わず、`locationValue` をそのまま使用しているか

---

## 技術的な詳細

### 重複抑制の仕組み

1. **DB制約**: `@@unique([userId, studyRecordId, type, nextReviewAt)`
   - データベースレベルで重複を防止

2. **アプリケーションレベルチェック**:
   ```typescript
   const existing = await prisma.notification.findFirst({
     where: {
       userId,
       studyRecordId,
       type: notificationType,
       nextReviewAt,
     },
   })
   
   if (existing) {
     return // 既に存在する場合はスキップ
   }
   ```

### 未読数の更新タイミング

1. **ページ遷移時**: `useEffect` の依存配列に `pathname` を追加
2. **ポーリング**: 10秒ごとに自動更新
3. **通知操作後**: `window.updateUnreadCount()` を呼び出し
4. **復習完了後**: 通知生成の可能性があるため更新

---

## 次のステップ（将来の拡張）

1. **リアルタイム更新**: WebSocketやServer-Sent Eventsを使用
2. **通知設定**: 通知タイプごとのON/OFF設定
3. **通知の自動削除**: 古い通知の自動削除
4. **プッシュ通知**: ブラウザのプッシュ通知API統合
