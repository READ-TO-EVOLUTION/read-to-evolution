# エラーメッセージ確認方法

## 1. ブラウザの開発者ツール（最重要）

### Chrome / Edge
1. ブラウザで `http://localhost:3000` を開く
2. **F12キー** または **右クリック → 検証** で開発者ツールを開く
3. **Console（コンソール）タブ** を確認
   - 赤いエラーメッセージが表示されます
   - エラーの詳細をコピーしてください

### Firefox
1. **F12キー** で開発者ツールを開く
2. **コンソール** タブを確認

### Safari
1. **Cmd + Option + I** で開発者ツールを開く
2. **コンソール** タブを確認

---

## 2. ターミナルのサーバーログ

開発サーバーを起動しているターミナルで、以下のようなエラーが表示されます：

```
Error: ...
  at ...
```

**確認方法:**
- ターミナルウィンドウを確認
- 赤いエラーメッセージを探す
- エラーのスタックトレース（at ... の部分）も重要

---

## 3. ネットワークタブ（APIエラーの場合）

1. 開発者ツールを開く（F12）
2. **Network（ネットワーク）タブ** を開く
3. ページをリロードまたは操作
4. 赤いステータスコード（400, 500など）のリクエストをクリック
5. **Response（レスポンス）タブ** でエラーメッセージを確認

---

## 4. サーバー側のログ確認

### ターミナルで確認
```bash
# 開発サーバーのログを確認
# エラーが表示されているはずです
```

### ログファイル（もしあれば）
- `.next` フォルダ内のログ
- または `npm run dev` の出力

---

## 5. よくあるエラーの種類

### ブラウザコンソール
- `TypeError: Cannot read property 'xxx' of undefined`
- `ReferenceError: xxx is not defined`
- `Failed to fetch` (API接続エラー)

### ターミナル（サーバー）
- `PrismaClientKnownRequestError` (DBエラー)
- `TypeError` (型エラー)
- `SyntaxError` (構文エラー)

---

## 6. エラーメッセージを共有する際のポイント

以下の情報を含めてください：

1. **エラーメッセージ全文**（コピー&ペースト）
2. **エラーが発生した場所**
   - ブラウザのコンソール？
   - ターミナルのサーバーログ？
3. **エラーが発生した操作**
   - どのページで？
   - 何をクリック/入力した時？
4. **スクリーンショット**（可能であれば）

---

## 7. 今すぐ確認できること

1. ブラウザで `http://localhost:3000` を開く
2. **F12** で開発者ツールを開く
3. **Consoleタブ** で赤いエラーを探す
4. エラーメッセージをコピーして共有してください
