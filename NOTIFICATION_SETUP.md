# 通知機能の動作確認手順

## 実装内容

通知機能（B-8）を実装しました。

### 最新の改善（UX向上）

1. **通知タイトル仕様準拠**
   - 表示形式: 「【復習】{書籍名}｜{location_value}」
   - location_value以外の接頭辞（「ページ」等）は付けない
   - location_valueが空の場合は「（位置未設定）」を表示

2. **重複抑制**
   - 同一ユーザーで (studyRecordId + type + nextReviewAt) が同じ通知は作成しない
   - DBのunique制約で重複を防止

3. **次回復習日時の表示**
   - 通知一覧に「次回復習: {日時}」を表示
   - 通常復習と再確認の区別は維持（色分け表示）

4. **全画面共通ヘッダー＋未読数バッジ**
   - すべてのページにヘッダーを表示
   - 未読通知数がバッジで表示される
   - 通知操作後、ページ遷移時に自動更新

### 実装した機能

1. **通知モデル（Prisma Schema）**
   - `Notification` モデルを追加
   - 通知タイプ: `review_normal`（通常復習）、`review_refresh`（再確認）
   - 既読/未読管理

2. **通知生成サービス**
   - `lib/notification-service.ts` - 通知生成ロジック
   - 表示形式: 「【復習】{書籍名}｜{location_value}」

3. **通知生成タイミング**
   - 復習実行時（Step3-6）にスケジュール更新と同時に通知生成
   - Step3到達時（Step1-2完了時）に通知生成
   - 通常復習と再確認を区別

4. **通知API**
   - `GET /api/notifications` - 通知一覧取得（未読数も返す）
   - `POST /api/notifications` - すべての通知を既読にする
   - `POST /api/notifications/[id]/read` - 個別通知を既読にする

5. **通知画面**
   - `app/notifications/page.tsx` - 通知一覧画面
   - 通常復習と再確認を色分け表示
   - 未読通知のハイライト
   - 個別既読・一括既読機能

---

## 動作確認手順

### 0. 初回セットアップ（初めて起動する場合）

初回起動手順は [VERIFICATION.md](./VERIFICATION.md#初回起動手順windows-powershell) を参照してください。

### 1. データベーススキーマの更新

```bash
# Prismaクライアントを再生成
npx prisma generate

# データベーススキーマを適用
npx prisma db push
```

**注意**: 既存のデータベースがある場合、`Notification` テーブルが追加されます。

### 2. 開発サーバーの起動

```bash
npm run dev
```

### 3. 通知生成の確認

#### ステップ1: 学習記録を作成

1. ログイン後、「教材管理」→ 書籍を登録
2. 「教材を登録」→ 学習記録を作成
   - 問題画像URL: `https://via.placeholder.com/400x300?text=問題1`
   - 位置情報: `p.45`
3. 学習記録作成後、Step1から開始

#### ステップ2: Step3到達時の通知確認

1. Step1-2を完了してStep3に到達
2. 「通知」画面（`/notifications`）にアクセス
3. 通知が表示されることを確認:
   - タイトル: 「【復習】{書籍名}｜ページ p.45」
   - タイプ: 「通常復習」（青色のバッジ）
   - 未読状態（青い点が表示）

#### ステップ3: 復習実行時の通知確認

1. 「今日の復習」画面で復習を実行
2. 復習を完了（「合っていた」または「合っていなかった」を選択）
3. 「通知」画面を確認
4. 新しい通知が追加されることを確認:
   - タイトル: 「【復習】{書籍名}｜ページ p.45」
   - タイプ: 「通常復習」または「再確認」（卒業済みの場合）

#### ステップ4: 再確認（REFRESH）の通知確認

1. Step6で復習を連続成功させて卒業（MASTERED）
2. 卒業後、再確認スケジュールが作成される
3. 「通知」画面を確認
4. 通知タイプが「再確認」（緑色のバッジ）になっていることを確認

#### ステップ5: 既読機能の確認

1. 通知一覧で「既読」ボタンをクリック
2. 通知が既読状態になることを確認（青い点が消える、透明度が下がる）
3. **ヘッダーの未読数バッジが減ることを確認**
4. 「すべて既読にする」ボタンをクリック
5. すべての通知が既読になることを確認
6. **ヘッダーの未読数バッジが消えることを確認**

#### ステップ6: 未読数の即時更新確認

1. 別のブラウザタブで通知を生成（復習を実行）
2. 元のタブに戻る
3. **10秒以内にヘッダーの未読数バッジが更新されることを確認**（ポーリング）
4. ページ遷移（「今日の復習」→「教材管理」など）を実行
5. **ページ遷移時に未読数が更新されることを確認**

---

## 確認ポイント

### ✅ 通知の表示形式
- 「【復習】{書籍名}｜{location_value}」形式になっているか
- 例: 「【復習】基本情報技術者試験 過去問集｜p.45」
- **「ページ」などの接頭辞が付いていないか確認**
- **次回復習日時が表示されているか確認**

### ✅ 通常復習と再確認の区別
- 通常復習: 青色のバッジ「通常復習」
- 再確認: 緑色のバッジ「再確認」

### ✅ 未読/既読の管理
- 未読通知: 青い点が表示、左側に青いボーダー
- 既読通知: 透明度が下がる、グレーのボーダー
- **ヘッダーの未読数バッジが正しく表示・更新されるか確認**

### ✅ 通知生成タイミング
- Step3到達時
- 復習実行時（スケジュール更新時）
- 卒業時（再確認スケジュール作成時）

---

## データベース確認（Prisma Studio）

```bash
npm run db:studio
```

ブラウザで [http://localhost:5555](http://localhost:5555) を開き、`notifications` テーブルを確認:

- `type`: `review_normal` または `review_refresh`
- `title`: 「【復習】{書籍名}｜{location_value}」
- `isRead`: `false`（未読）または `true`（既読）
- `studyRecordId`: 関連する学習記録ID

---

## トラブルシューティング

### 通知が生成されない

1. **データベーススキーマが更新されているか確認**
   ```bash
   npx prisma db push
   ```

2. **Prisma Studioで確認**
   - `notifications` テーブルが存在するか
   - 通知レコードが作成されているか

3. **ブラウザのコンソールでエラー確認**
   - ネットワークタブでAPIリクエストのエラーを確認

### 通知が重複する

- 現在の実装では、スケジュール更新のたびに通知が生成されます
- 必要に応じて、重複チェックロジックを追加できます

### 位置情報の表示がおかしい

- `location_type` が正しく設定されているか確認
- `location_value` が空でないか確認

---

## 次のステップ（将来の拡張）

1. **プッシュ通知**
   - ブラウザのプッシュ通知API統合
   - サービスワーカー実装

2. **メール通知**
   - メール送信サービス統合（SendGrid等）
   - 通知設定（メール通知ON/OFF）

3. **通知設定**
   - 通知タイプごとのON/OFF設定
   - 通知頻度の調整

4. **通知の自動削除**
   - 古い通知の自動削除（30日以上経過など）
